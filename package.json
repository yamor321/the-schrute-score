{
  "name": "the-schrute-score",
  "version": "1.0.0",
  "private": true,
  "description": "Which AI coding model gives the most coding capability per dollar? An auto-updating dashboard built on Artificial Analysis data.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "update": "tsx src/update.ts",
    "copy-data": "tsx scripts/copy-data.ts",
    "preview": "tsx scripts/serve.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
